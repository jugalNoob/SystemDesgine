1️⃣ What is Replication in MongoDB?

Replication means copying data across multiple servers so that:

If one server fails, data is still available on another server.

Reads can be distributed across replicas for better performance.

Automatic failover is possible when the primary node goes down.